spring:
  application:
    name: resume
  datasource:
    url: jdbc:postgresql://localhost:5432/resume_database
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql:true
